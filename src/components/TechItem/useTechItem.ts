import { TechItemPropType } from "./types";

const iconProps: TechItemPropType = {
  size: "20px",
  color: "#ffffff",
};

const useTechItem = () => {
  return {
    iconProps,
  };
};

export default useTechItem;
